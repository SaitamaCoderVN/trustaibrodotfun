[toolchain]
package_manager = "yarn"

[features]
resolution = true
skip-lint = false

[programs.devnet]
trustaibro_program = "4ke9FgyExgpUFnct3cPAcFT94tCwSPPXTts9Y1QdHXNK"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"
# Use custom RPC endpoint to avoid rate limits
# You can use Helius, QuickNode, or other RPC providers
# Uncomment and set your RPC URL if needed:
# url = "https://api.devnet.solana.com"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
